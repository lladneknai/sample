import { styled } from "@mui/material/styles";
import Box from "@mui/material/Box";

const FileTreeContainer = styled(Box)(() => ({
  backgroundColor: "#0ff2",
  minHeight: 352,
  minWidth: 250,
}));

export default FileTreeContainer;
